# 论文推荐 Web 应用设计（头脑风暴稿）

> 状态：**需求规格**；M1 已实现，完整目标界面以 [design/ 原型](docs/design/prototype.html) 为最新定稿。
> 设计日期：2026-07-29；状态更新：2026-09-13（见 [需求与现状审阅报告](docs/product/review.md)）。
> 定位：本文档是「需求规格 + 自研路线图」，独立成立；是否动手实现由 [survey.md](survey.md) 的调研结论决定。
> 现有系统（CLI + agent）见 [README.md](README.md)；本文所有设计以其数据与工作流为地基。

## 1. 愿景与设计原则

**一句话产品定义**：研究者个人的、画像可审计的、多源论文推荐与归档工作台
（a personal, auditable-profile, multi-source paper recommendation & archiving workbench）。

四条设计原则（贯穿所有功能取舍）：

1. **本地优先（local-first）**：数据全部落在本仓库内，可 Git 版本化、可离线、可随时退出；
   不引入云端账号体系。
2. **Markdown/JSON 为唯一事实源**：Web 应用只是「视图层」，`interests.md`、
   `data/*.json`、`feedback.jsonl`、`arXiv-schedule.md` 仍是权威数据，
   文件可随时手工编辑，Web 端读写的就是这些文件。
3. **CLI 永远是 fallback**：Web 挂了脚本照跑；任何 Web 功能都不得使 CLI/Obsidian
   工作流失效。
4. **可解释推荐**：每条推荐必须能回答「为什么推荐给我」——命中了哪个画像条目、
   权重得分多少、agent 给出什么理由。拒绝黑盒。

## 2. 用户与核心场景

单用户（本仓库所有者），三个核心场景，场景直接映射页面：

| 场景 | 现状（CLI + 手工） | Web 化后 | 对应页面 |
|------|------------------|---------|---------|
| S-A 晨间浏览打分 | 打开 arXiv-schedule.md 逐行读表格，调分需敲 `feedback` 命令 | 卡片流 j/k 翻阅，按 1-5 即打分即生效 | Digest 流 |
| S-B 画像维护 | 手工编辑 interests.md，或让 agent 解析主页/论文/仓库链接 | 表单化编辑 + 权重滑块 + 演化时间线 | 兴趣画像 |
| S-C 下载归档 | 敲 `download <id>` | 卡片上一键下载，自动按命名规则落盘 | Digest 流 / 归档库 |

## 3. 数据层（零迁移）

Web 应用**没有自己的数据库**，直接复用现有三件套作为初始数据层；
如需加速检索，仅允许增加**可随时删除重建**的只读索引缓存（如 SQLite/whoosh）。

### 3.1 现有数据资产与精确 schema（与 `arxiv_daily.py` 实际行为一致）

- `data/YYYYMMDD.json` — 顶层 `{day, window_utc[2], papers[]}`；每篇论文字段：
  `id`（如 `2607.25979`）、`version`、`title`、`authors[]`、`abstract`、
  `primary`、`categories[]`、`published`、`abs_url`、`pdf_url`；
  若 `interests.md` 存在，fetch 时另写入 `hits_star[]`、`hits_expand[]`、
  `score_star`、`score_expand`（命中条目权重和）。
  注意：早期数据文件（如 `20260729.json`）不含 hits/score 字段，解析须容错。
- `interests.md` — 画像真相源。`##` 分兴趣点(★)/扩展点(🧐)两节；
  每条目 = `###` 标题 + `- keywords:`（分号分隔）+ `- weight: N`（1-10，缺省 3）
  + `- 来源:`。Web 端解析渲染、写回仍是同一 md 文件。
- `feedback.jsonl` — append-only 事件日志（lazy 创建）。每行
  `{time, id, day, title, primary, abstract_head, adjustments[], weight_updates{}}`，
  其中 `adjustments[] = {kind, rating, delta, matched_entries[], digest_updated}`。
- `arXiv-schedule.md` — 人读视图（Obsidian/GitHub），继续由脚本生成，Web 不依赖它做数据源。

### 3.2 统一 Paper 实体（多源扩展的前提）

引入多源后同一论文可能来自 arXiv、DBLP、OpenReview 等，需要统一实体与去重：

- 主键优先级：DOI > arXiv id > Semantic Scholar id > OpenAlex id >（标题+作者 模糊指纹）。
- 每篇论文记录 `sources[]`（出处列表）与 `ids{}`（各源 id 映射），推荐/反馈/下载均针对统一实体。
- 落盘形态不变：仍是每日 JSON + 可选索引缓存，不因多源引入数据库。

## 4. 功能域头脑风暴

十个功能域。每域分 **MVP / 增强 / 远期** 三档，并标注实现策略：
**自研** / **借 API**（调外部数据服务）/ **外链**（跳转现成工具，不自研）。

### 4.1 多源采集（打破「仅 arXiv」限制）　[自研接口 + 借 API]

- **MVP**：定义插件式 source adapter 接口
  （`fetch_window(start, end) -> [Paper]`，声明式配置源与类别/venue 过滤），
  arXiv 适配器 = 现有 `fetch` 逻辑重构；另实现 **1 个示例源**验证接口
  （首选 DBLP：CS venue 权威，且与本仓库 300+ venue 目录天然对齐）。
- **增强**：
  - Semantic Scholar / OpenAlex 适配器：补引文数、SPECTER 嵌入、开放获取 PDF 链接；
  - OpenReview 适配器：ICLR/NeurIPS 等的审稿评分——独特的质量信号；
  - Crossref DOI 解析：任意 DOI 粘贴即入库；
  - RSS/Atom 通用适配器：会议 CFP、期刊 TOC、博客。
- **远期**：学者主页监控（关注作者新作，diff 主页出版物列表）；
  ACL Anthology、HAL 等区域/领域源按需接入。
- 每个源标注配额与降级路径（S2 需 API key 提额；OpenAlex polite pool 仅需邮箱；
  DBLP 免费）；不做任何绕过反爬的方案。

### 4.2 画像与推荐算法演进　[自研 + 借 API]

分四级渐进，任何一级都可解释、可关停回退到上一级：

- **L0 关键词命中（现状，MVP）**：interests.md keywords 整词匹配 + 条目权重求和
  （`score_star/score_expand`）。
- **L1 嵌入相似（增强）**：interests.md 每条目向量化（条目标题+keywords+种子论文摘要），
  论文用 SPECTER2 嵌入（S2 API 提供，或本地 sentence-transformers），
  余弦相似 × 条目权重。解释形式：「与条目 X 相似度 0.83」。
- **L2 LLM 语义评分（增强）**：现有 agent 层 Web 化——对 L0/L1 初筛后的 top-N
  调用 LLM 打分并生成一句中文推荐理由（供应商可配置，本地模型优先）。
- **L3 混合排序（远期）**：`最终分 = w1·L0 + w2·L1 + w3·L2 + 反馈时间衰减项`，
  权重可在设置页调节并 A/B 对照。
- ★/🧐 双通道始终分离：兴趣点条目只产生 ★ 候选，扩展点条目只产生 🧐 候选——
  「探索/利用」不混流（这是本系统区别于所有现成产品的核心机制，见 survey.md §6）。

### 4.3 反馈闭环（画像演化）　[自研]

- **MVP**：卡片上一键 0-5 ★/🧐 打分，直接调用现有 `feedback` 逻辑
  （digest 表格 patch + 条目权重 ±1 + feedback.jsonl 追加），行为与 CLI 完全一致。
- **增强**：
  - 隐式信号：skip（划过不读）/ save（收藏）/ read（点开 PDF）/ download，
    以较小步长影响权重（如 ±0.2，落 feedback.jsonl 新事件类型）；
  - 权重演化时间线：回放 feedback.jsonl，展示每个条目权重随时间的折线；
  - 高分未命中画像的论文 → 「建议新增条目」收件箱（对应现有 CLI 的 HINT 机制）。
- **远期**：周期性「画像体检」报告——哪些条目权重持续漂移、哪些 keywords 从未命中、
  建议合并/拆分/退休条目（报告落盘为 Markdown，可让 agent 执行建议）。

### 4.4 阅读工作流　[自研 + Obsidian 协同]

- **MVP**：阅读队列三态 inbox → reading → done/archived（状态存本地 JSON）。
- **增强**：内嵌 PDF 阅读器（PDF.js）+ 高亮标注；每篇论文一页 Markdown 笔记，
  落盘到仓库（本仓库已是 Obsidian vault，笔记直接可被 Obsidian 打开/反链）。
- **远期**：标注导出为结构化摘录、TTS 听摘要/速读模式。

### 4.5 PDF 库集成（本仓库独有资产）　[自研]

- **MVP**：下载沿用现有命名规则 `arXiv<年份> <id><版本> <去标点标题>.pdf`
  （含 LaTeX 记号清理），落 `arXiv/` 目录；非 arXiv 源论文按仓库
  `Conf/Journal + Year + Title` 惯例落对应 venue 目录。
- **增强**：全库扫描建索引——venue/年份统计面板、按文件名的全库检索、
  重复文件检测。
- **远期**：「库内已有相关论文」提示——新推荐论文的作者/引文与库内 PDF 匹配时，
  卡片上给出「你的库里已有该作者 3 篇」；全文检索（对 PDF 抽取文本建索引）。

### 4.6 引文图谱　[外链为主，V2 轻量自研]

- **MVP**：**外链** Connected Papers / Litmaps / Semantic Scholar 页面
  （卡片上一个按钮，不自研图谱——现成工具已足够好，见 survey.md）。
- **增强**：借 S2/OpenAlex 引文数据，做**轻量本地关系图**：仅展示
  「本次推荐论文 ↔ 库内已有论文」的引用连边（帮助判断是否值得读）。
- **远期**：不做通用引文图谱探索（明确非目标，见 §7）。

### 4.7 AI 摘要 / 问答 / 周报　[自研编排 + LLM 可配置]

- **MVP**：不做（现有 digest 的两句摘要节选已够用）。
- **增强**：
  - 单篇结构化摘要卡片：问题/方法/结果/与我的相关性 四栏（LLM 生成，缓存落盘）；
  - 每周趋势周报：汇总本周 digest + 反馈，生成 Markdown 周报落盘
    （「本周你给 X 领域打了 N 个高分…」）。
- **远期**：库内 RAG 问答（对已下载 PDF 提问，引用页码作答）；跨篇对比表格。
- 约束：LLM 供应商可配置、本地模型优先、所有生成物落盘为 Markdown（可审计、可 Git）。

### 4.8 协作与分享　[自研导出，社交外链]

- **MVP**：导出精选清单为 Markdown（现有 digest 即是）。
- **增强**：导出 HTML/RSS feed（他人可订阅你的每周精选）；
  引入外部热度信号作为参考列（alphaXiv trending、HuggingFace Daily Papers 票数）。
- **远期**：组内共享画像/清单（多人场景，优先级最低——见 §7 非目标边界）。

### 4.9 通知渠道　[自研]

- **MVP**：无独立推送渠道。**调度已就位**：仓库内 `daily` 命令（fetch → 确保 Web
  服务在线）+ `scripts/register_task.ps1`（Windows 计划任务），或 WorkBuddy 平台
  定时任务——由 agent 抓取、填写推荐、`present_files` 呈递页面。见
  [docs/user-guide/scheduling.md](docs/user-guide/scheduling.md)。
  **推送框架已实现**（供 §4.11 复用）：`glean/notify.py` 的
  `file` / `desktop` / `webhook` 三通道，webhook 支持 `generic` / `feishu` / `wecom`。
- **增强**：每日 digest 邮件（本地 SMTP 配置）；Telegram / 企业微信 webhook
  推送「今日 ★≥4 论文 N 篇」摘要卡。
- **远期**：RSS 输出端点（本地服务暴露 `/feed.xml`）。

### 4.10 移动端 / PWA　[自研]

- **MVP**：响应式布局（手机浏览器可用即可）。
- **增强**：PWA——离线缓存当日 digest，通勤时刷卡片打分，回网后同步写回
  feedback.jsonl（冲突策略：append-only 日志天然免冲突）。
- **远期**：推送通知（依赖 PWA push，本地服务场景受限，低优先级）。

### 4.11 监控与推送　[自研，M1.x 已实现]

> 与 §4.9 的分工：§4.9 是「arXiv 日报**怎么送达到你**」；本节是「**盯人**——
> 按学者而非类别跟踪新作」。两者共用 `glean/notify.py` 的推送框架。

- **MVP（已实现）**：
  - 名单 `watchlist.md`（Markdown 真相源）：姓名 / 主页 / DBLP / S2 / 标签 / 启停；
    CLI、Web 表单、直接编辑三种增删方式。
  - 解析顺序 **个人主页 → DBLP → Semantic Scholar**。主页优先，因其唯一能覆盖
    「视频 / 技术报告 / talk」（S2 与 DBLP 只收录论文）；主页无结果时兜底，
    显式配置了 id 时并行合并去重。
  - **不做 Google Scholar**：无官方 API、抓取违反其 ToS 且极易被封禁；
    以 DBLP + Semantic Scholar 的公开 API 达到同等覆盖。
  - 差异判定：指纹 = sha1(规范化标题 + 可选 URL 主机指纹)；**首次运行建基线不推送**，
    之后只推未见过条目（`--force` 可强制）。
  - 推送四通道：`file`（`WATCH-digest.md` + Web NEW 徽标）、Web 高亮、
    `desktop`（Windows 气泡）、`webhook`（`generic`/`feishu`/`wecom`）。全部 fail-soft。
  - Web：新增 `/watch` 页面（名单增删 + NEW 徽标 + 推送历史）与 `/api/watch/*`。
- **增强**：
  - 主页解析支持可选 JS 渲染兜底（仅对 JS 型站点启用）；
  - 按 `tags` 订阅推送（只推某些标签的新作）；
  - 用 S2 补全主页条目的 abstract / venue / 引用数。
- **远期**：
  - 监控对象自动发现（从 `feedback.jsonl` 高分论文作者反推「值得盯的人」）；
  - 与 arXiv 日报合并为单一「今日简报」。
- **已知边界（判断）**：主页解析是启发式的，置信度 < 0.5 一律丢弃——
  偏向**漏报**而非误报；JS 渲染型主页只能靠 DBLP/S2 兜底。

### 4.12 CCF-A 会议 / 期刊监控与推送　[自研，M1.x 已实现]

> 与 §4.11 的分工：§4.11 是「盯**人**」；本节是「盯**会议/期刊的节奏**」——
> CFP 放出、Program 公布、接收论文列表上线。三者互补，互不重叠。
> 已实现（2026-09-14）。参考实现：[ccfddl.com](https://ccfddl.com/)。

- **MVP（已实现）**：
  - 名单 `ccf.md`（Markdown 真相源，**勾选框**）：勾选即订阅、取消即暂停；
    CLI（含 `--area` 批量）/ Web（可点勾选框 + 按领域全选/全不选）/ 直接编辑三种方式。
  - 目录 `glean/ccf_catalog.py`（快照，可由 `scripts/gen_ccf_catalog.py` 重新生成）：
    **会议**取 ccfddl 公开 RSS（带 CCF 等级 / 领域 / 官网 / DBLP 索引，同会议取最新一届官网），
    **期刊**为人工整理的 A 类期刊并按 ISSN 到 Crossref 校验。当前 71 会议 + 22 期刊。
  - 解析顺序：会议 = **ccfddl 截稿 RSS → 会议主页**；期刊 = **Crossref（按 ISSN 取卷期）→ 期刊主页**。
    分工理由：CFP 有结构化来源就不该猜 HTML；Program 与接收论文列表只存在于会议主页；
    期刊主页（ACM DL / IEEE Xplore）是 JS 渲染的空壳，故改走 Crossref 开放 API。
  - 三个信号：`cfp`（征稿）/ `program`（日程）/ `papers`（接收论文列表），
    每条带 `confidence`，< 0.5 丢弃，`0.5–0.6` 标「⚠️ 低置信」。
  - 差异判定：指纹 = sha1(规范化标题 + URL 主机指纹)；**首次运行建基线不推送**。
    期刊指纹刻意锚定 `(venue, volume, issue)`、URL 固定用期刊主页——
    同一期持续有新文章入库不重复播报，只有**新卷新期**才算事件。
  - 推送复用 §4.11 的四通道（`file` / Web 高亮 / `desktop` / `webhook`），
    但走**独立命名空间**：`data/ccf_new.json` + `CCF-digest.md`，与学者监控的未读互不清除。
  - Web：新增 `/ccf` 页面与 `/api/ccf/*` 端点；导航「CCF」显示自身未读计数。
- **增强**：
  - 会议主页 JS 渲染兜底；按 `area` 分别配置推送渠道；
  - 与 arXiv 日报交叉：把「接收论文列表」里的标题与当日 arXiv 新论文做匹配（提前发现）；
  - 会议投稿截止日临近提醒（ccfddl 已提供 deadline，可做 T-7/T-1 提醒）。
- **远期**：
  - 目录自动跟随 ccfddl 更新（当前为快照 + `ccf refresh` 手动同步）；
  - 把三条监控线合并为单一「今日简报」。
- **已知边界（判断）**：
  - **DBLP 已不可抓取**（2026-09 起全部端点返回 Anubis 反爬拦截页），
    因此 `dblp` 字段仅为人工参考链接，§4.11 的 DBLP 兜底源目前会静默返回空
    （fail-soft，不报错但覆盖下降）；
  - 期刊目录是人工整理的，是目录里**最可能出错**的部分，出错的条目取消勾选即可；
  - 全量扫描 93 个条目约需数分钟，`--area` 批量退订是主要的成本控制手段。

## 5. 现代界面设计

### 5.1 信息架构：五个页面

```
┌──────────────────────────────────────────────────────────────────┐
│  ⌘K 命令面板  [Digest 流] [兴趣画像] [归档库] [学者] [CCF]   🌙 暗色 │
├────────────┬────────────────────────────────────┬────────────────┤
│ 日期导航    │  卡片流（虚拟滚动）                  │ 详情面板        │
│ 2026-07-29 │ ┌────────────────────────────────┐ │ 标题/作者/摘要  │
│ 2026-07-28 │ │ ★★★★☆  Hermes: BFT Consensus…  │ │ ────────────  │
│  …         │ │ cs.DC · 🎯★ consensus, BFT     │ │ 为什么推荐:     │
│ 过滤器      │ │ 为什么推荐: 命中「分布式共识」   │ │ · 命中条目+权重 │
│ ☑ ★ 候选   │ │ (w=3, score=3) + agent 理由…   │ │ · agent 理由    │
│ ☑ 🧐 候选  │ │ [1-5 打分] [📥 下载] [🔗 图谱]  │ │ ────────────  │
│ ☐ 其余     │ └────────────────────────────────┘ │ PDF 预览        │
│ 类别筛选    │ ┌────────────────────────────────┐ │ (PDF.js)       │
│ cs.DB ✓    │ │ 🧐🧐🧐  MemLens: Memory for …   │ │                │
│ cs.DC ✓    │ │ …                              │ │                │
└────────────┴────────────────────────────────────┴────────────────┘
```

- **Digest 流**（首页）：左=日期与过滤器；中=卡片流；右=选中论文详情+PDF 预览。
- **学者**（M1.x 新增，`/watch`）：上=新作列表（NEW 徽标、按人分组、类型图标
  📄/🎥/📕/🎤）；中=名单增删表单与条目（暂停/移除）；下=推送历史。
  导航「学者」角标显示未读条数。
- **CCF**（M1.x 新增，`/ccf`）：上=新动态列表（NEW 徽标、类型图标 📢/📅/📄）；
  中=按领域的「全选 / 全不选」批量按钮 + 勾选框名单（主页 / DBLP / 移除）；
  下=推送历史。导航「CCF」角标显示自身未读条数（与「学者」互不干扰）。
- **兴趣画像**：条目卡片（keywords 标签化编辑、权重滑块 1-10、来源链接）+
  权重演化时间线 + 「建议新增条目」收件箱。

```
┌──────────────────────────────────────────────────────────────────┐
│ 兴趣点 ★                                      [+ 新增条目]        │
│ ┌──────────────────────────────┐  权重演化                        │
│ │ 分布式计算与共识      w=4 ▮▮▮▮ │  4 ┤      ╭──                  │
│ │ consensus ×  BFT ×  Paxos ×  │  3 ┤──────╯                     │
│ │ 来源: hengxin.github.io      │    └───────────────▶ 时间        │
│ └──────────────────────────────┘  建议收件箱: 「LLM 形式化验证」   │
│                                   高分未命中 2 篇 → [采纳][忽略]  │
└──────────────────────────────────────────────────────────────────┘
```

- **归档库**：venue/年份统计面板 + 全库文件检索 + 最近下载列表。

### 5.2 关键交互

- **卡片即答案**：每张卡片自带「为什么推荐」（命中条目 + 权重得分 + agent 中文理由），
  🎯 命中词在摘要中高亮。
- **全键盘流**：`j/k` 上下张卡片、`1-5` 打 ★、`Shift+1-5` 打 🧐、`d` 下载、
  `s` 收藏、`o` 打开 arXiv 页、`n` 记笔记、`/` 搜索、`⌘K` 命令面板。
- **打分即生效**：打分后卡片角标即时更新权重变动提示
  （「分布式计算与共识 3→4」），无确认弹窗，feedback.jsonl 可撤销回放。
- **暗色模式**默认跟随系统；阅读密度可切换（舒适/紧凑）。
- **性能**：cs.LG 洪峰日单日数百篇 → 卡片流虚拟滚动；摘要懒展开。
- **可视化面板**（Digest 流顶部可折叠）：兴趣权重雷达图、近 30 日每日新增量趋势、
  venue 分布、反馈历史时间线。

## 6. 技术架构展望（只到选型层）

| 方案 | 组成 | 优点 | 缺点 |
|------|------|------|------|
| **本地单进程（推荐）** | FastAPI + HTMX/Alpine（或 Flask），SQLite 只读索引，PDF.js；`pip install` 一条命令起服务 | 零运维、数据不出机、与现有 Python 资产同栈、CLI 共享同一套函数 | 多设备需 Git 同步 |
| 本地前后端分离 | SvelteKit/React 前端 + FastAPI 后端 | 界面上限高（虚拟滚动/动画更顺） | 构建链重，维护成本↑ |
| 云端 SaaS | 托管服务 + 账号体系 | 随处可用、可分享 | 违背本地优先原则，数据主权丧失，明确不选 |

**迁移兼容承诺**（无论选型如何都必须满足）：

1. `arxiv_daily.py` 重构为「采集/反馈核心库 + CLI 壳」，Web 与 CLI 调同一套函数，
   逻辑不重写、不分叉；
2. `interests.md` / `feedback.jsonl` 双向同步：文件手工编辑后 Web 端自动重载
   （监听文件变更），Web 端修改即写回文件；
3. `arXiv-schedule.md` 继续生成——Obsidian 阅读、Git diff 审阅、agent 填表
   三个既有工作流零破坏。

## 7. 非目标（Won't）

| 不做什么 | 理由 |
|---------|------|
| 多用户 / 社交 / 评论 | 单人工作台定位；社交讨论外链 alphaXiv 即可 |
| 云托管与账号体系 | 违背本地优先与数据主权原则 |
| 通用引文图谱探索 | Connected Papers / Litmaps / ResearchRabbit 已做得足够好，外链之（见 survey.md §4） |
| 通用 AI 文献问答 / 系统综述工具 | Elicit / Undermind 已覆盖，按需外用；本应用只做库内 RAG（远期） |
| 改变 PDF 落盘命名规则与 venue 目录结构 | 这是本仓库最重要的既有资产与不可替代特色（survey.md H3） |
| 自建爬虫绕过官方 API | 只用官方 API/RSS 并遵守配额礼仪 |

## 8. 分期路线图

每期独立有价值、可停在任一期（停在 M0 = 维持现状，也是合法终点）：

| 期 | 内容 | 交付判据 | 对应功能域 | 状态 |
|----|------|---------|-----------|------|
| M0 | 现状：CLI + agent + Markdown | （已完成） | — | ✅ 已完成 |
| M1 | 只读 Web 视图 + 打分/下载按钮 | 晨间浏览打分不再打开编辑器/敲命令 | 4.3 MVP、5.1、5.2 | ✅ 已实现（§5 部分交互待补，见 review.md §5） |
| M2 | 多源接入 + 嵌入推荐 | DBLP 源上线；L1 相似分数上卡片 | 4.1、4.2 L1 | 📝 规划中 |
| M3 | 阅读流 + 库集成 | 阅读队列可用；归档库统计面板 | 4.4、4.5 | 📝 规划中 |
| M4 | 图谱连边 / 周报 / 通知 | 库内关系图；每周 Markdown 周报 | 4.6 V2、4.7、4.9 | 📝 规划中 |

## 9. 开放问题

1. **LLM 成本与隐私**：L2 语义评分每日调用量 = top-N × 每篇一次；本地模型
   （如 Qwen 系列）能否达到可用质量？需要 M2 期做一次盲测对比。
2. **多设备同步**：坚持「Git 即同步」是否够用？手机 PWA 离线打分 →
   feedback.jsonl append-only 天然免冲突，但 interests.md 编辑冲突需约定
   （建议：移动端只读画像）。
3. **S2 API 配额**：无 key 100 req/5min（共享池），申请 key 后 1 req/s——
   嵌入与引文补全需按日批量拉取并缓存，配额是否足够需在 M2 验证。
4. **DBLP 无摘要**：DBLP 只有元数据，摘要需回填（Crossref/S2/出版商 OA 链接），
   回填失败时卡片如何降级展示？
5. **反馈步长**：隐式信号 ±0.2 与显式 ±1 的比例是拍脑袋值，需要积累
   feedback.jsonl 数据后回放调参。

---

*本文档由头脑风暴产生，功能取舍以 §1 四原则与 §7 非目标为准；
是否开工由 [survey.md](survey.md) 决策框架给出的结论决定。*
