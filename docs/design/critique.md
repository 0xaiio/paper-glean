# 质量审查报告 · Paper-Glean（摘星 StarDigest）高保真原型

> 审查对象：`docs/design/prototype.html`（单文件，**约 1294 行**，内嵌 JSON 数据集）
> 审查人：严过审（critique-reviewer）｜阶段：Phase4｜评审框架：5 维评审 + Anti-Slop 门控
> 基准文件：`requirements-brief.md`（§4/§5/§6）、`design-system.md`（v1.1 §2/§3/§4/§7/§9 + 附录 A）、`index.md`（§3/§5）
> **判定：PASS**（总分 **24/25**；全维 ≥3；**P0 = 0**；**P1 = 0**）
> 全部 P0/P1 均已修复并经 **Edge 无头 + CDP 实测**复验；本报告仅审查，未改动原型。
> ⚠ **行号已按最终 1294 行版本复标**。

---

## 0. 验证方式与边界

| 验证手段 | 结果 |
|---------|------|
| 逐行静态阅读 HTML/CSS/JS | ✅ 完成（全文） |
| 内嵌数据集解析（`pg-data`） | ✅ 128 篇真实论文；5 个真实兴趣条目（w=3） |
| 与真实源数据比对（`data/20260729.json`、`interests.md`） | ✅ 条目/权重/论文标题一致 |
| JS 语法校验（`node --check`） | ✅ 通过 |
| 逻辑执行验证（最小 DOM 桩执行 app.js：加载 + 视图切换 + 选中 + 打分 + 下载 + 洪峰 + 空态 + 键盘 handler；修复后复跑） | ✅ 无异常 |
| 外部请求静态扫描（`@import`/`<link>`/`fetch`/`cdn`/`src=http`） | ✅ 零命中 |
| **真实浏览器渲染 / 交互实测（Edge 无头 + CDP，主理人补做）** | ✅ **已完成**（本机无 Playwright/Puppeteer，但 `msedge.exe --headless=new --remote-debugging-port` 可用） |

> 结论：静态检查 + 数据核对 + 逻辑执行 + 真实浏览器实测**均已覆盖**。CDP 实测另捕获两处**静态检查与 DOM 桩均漏检**的运行时缺陷（P0-2 `bindControls()` 漏调用、C1 `renderStream()` 漏 `renderDetail()`，见 §5）；DOM 桩盲区（`addEventListener` 空操作）已记录。**「无头浏览器实测」应作为原型交付的强制关卡。**

---

## 0.1 修复闭环（v1.1 + CDP · 全部 P0/P1 已关闭）

| # | 问题 | 修复 | 复验证据 |
|---|------|------|---------|
| **P0-1** | 对比度不达标（白字按钮 3.68:1；tertiary 小字 3.6–3.9:1） | DS 升 v1.1：新增 `--accent-fill #2563EB`、提亮 `--text-tertiary → #8B94A3`/`#5F6773`；原型同步令牌（`28`/`34`/`69`/`105`/`222`） | CDP：暗色 tertiary **5.87/5.40:1**、亮色 **5.72/5.14:1**、主按钮白字 **5.17:1**，全 ≥AA ✅ |
| **P0-2** | `bindControls()` 定义完整却**从未调用** → ~16 个控件静默失效 | `prototype.html:1284` 新增 `bindControls();`（定义 `1159`，唯一调用） | CDP：视图/主题/密度/数据集/筛选/选中**全部生效** ✅ |
| **P0-3（C1）** | `renderStream()` 正常分支只 `renderWindow()`、**不 `renderDetail()`** → 所有"清空选中"路径（重置/切类别/切日期/切数据集/搜索/取消勾选）后中栏无选中卡，**右栏却残留上一张详情** | `renderStream()` 内 `renderWindow();`（`817`）之后新增 `renderDetail();`（**`818`**） | CDP：点「重置演示状态」后 `[data-selected="true"]`=**0**、`.detail-empty` 存在（修复前 `ABSENT`）、右栏文案「选择一篇论文查看详情」；切类别→空态；切洪峰日→384 篇且右栏空态；console **0 报错** ✅ |
| **P1-1** | `⌘K` 未加「规划中」视觉区分 | builder 已加**虚线描边 + `<span class="planned">规划中</span>`**（`482` 起） | CDP：`cmdkPlanned:true`、`densityPlanned:false` ✅ |
| （默认值，已修） | `state.showOther` 默认 `true`，与未勾选的 `#fOther`（`550`）及真实实现 `routes.py:35 show_other=False` 冲突 | 默认改为 `false`（`708`） | 静态核验：默认筛选状态与控件勾选一致 ✅ |

> **要点**：
> 1. **P0-2 / P0-3 是本流程最值得记录的教训**：两处均为"定义完整、接线遗漏"，导致功能静默失效/状态不一致，我的静态阅读与 DOM 桩均未捕获，由主理人 CDP 实测发现——**「无头浏览器实测」应成为原型交付强制关卡**。
> 2. **更正记录**：此前我误将「阅读密度切换」列为规划项——**错误**。CDP 实测点击「紧凑」→ `data-density` `comfortable→compact`、卡高 192→142px、写 `localStorage.pg_density`，**密度切换是已实现功能**，不得加「规划中」。
> 3. 其余结论（十态、键盘、为什么推荐、令牌忠实度、Anti-Slop）不受影响，仍全部成立。

---

## 1. 五维评分

| 维度 | 评分 | 理由 | 关键证据（行号） |
|------|------|------|-----------------|
| **哲学 Philosophy** | **5/5** | 彻底贯彻「工具先于装饰 / 数据密集 / 暗色优先 / 键盘原生 / 克制」。首屏即三栏工作台，无 Hero、无营销语；暗色默认且首帧无闪烁；顶部常驻快捷键提示条；一切数据走等宽。是「每天要用的工作台」。 | 主题前置 `15–21`；三栏栅格；快捷键条；等宽数据 `95` |
| **层次 Hierarchy** | **5/5** | 任务书点名的「系统判断 vs 我的判断」三重区分到位：**形态**（顶部实心 pill vs 底部 5 格 pips）、**位置**（卡顶右 vs 卡底）、**填充**（满饱和 vs 仅填已选格）；命中通道左色条可辨；选中态用「左条 + 底纹 + inset 描边」三段式，非阴影堆叠。 | 命中徽标；pips；卡片组合 `772–797`；选中态 `card[data-selected]` |
| **执行 Execution** | **4/5** | 令牌逐字采用、**未自创色值**（70 个色值全部命中 `design-system v1.1 §9.4`）；`ring:` 无效属性 **0 处**；`prefers-reduced-motion` 双处降级；`:focus-visible` 焦点环到位；`aria-*` 齐备；零外部请求；JS 语法与执行路径通过。**扣分项**：首版含两处运行时缺陷（P0-2 `bindControls()` 漏调用、P0-3/C1 `renderStream()` 漏 `renderDetail()`，均已修复），暴露**交付前自测关卡不足**；另含 `--card-pad` 未消费、主题写入冗余等 P2。 | 令牌 `26–84`；`bindControls` 定义 `1159`／调用 `1284`；C1 修复 `818`；色值扫描见 §3 |
| **特异性 Specificity** | **5/5** | 内容真实：128 篇取自 `data/20260729.json`（含 `Hermes…` 等真论文）；兴趣条目逐字来自 `interests.md`（5 条、w=3）；无 Lorem、无假人名。**「为什么推荐」真正渲染**：`hs/he` 存的是**命中条目标题**且与 `INTERESTS.title` 完全匹配，`weightOf()` 可命中 → 实测卡片输出含「分布式计算与共识 w=3」（已避开历史 `hits_*` 存关键词导致永不渲染的坑）。双通道分离正确。 | 数据；`weightOf` `694`；`whyTags` `761`；`cardHTML` `772` |
| **克制 Restraint** | **5/5** | 未触发 Anti-Slop 十条禁令任一条（见 §2）。无渐变（仅骨架功能性微光）、无玻璃拟态、卡片圆角 6px、功能图标全线性 SVG、暗色下用描边而非重阴影、单一品牌蓝 + 双语义通道、无营销 Hero。规划项以**虚线「规划中」徽标**与已实现区分。 | 圆角 `49`；图标 SVG `672` 起；阴影仅弹层；`.planned` `150` |

**总分：24 / 25**
**门控：** 维度全部 ≥3（通过）｜**P0 = 0**｜**P1 = 0**（≤3）→ **结论 PASS**

---

## 2. Anti-Slop 门控结论

| 检查项 | 结论 | 说明 |
|--------|------|------|
| 紫色/彩虹渐变 | ✅ 无 | 全站纯色填充，唯一渐变是 `.skeleton` 功能性加载微光（§7 例外） |
| 编造数据/虚假证言 | ✅ 无 | 128 篇真论文、5 条真兴趣、真实 arXiv id/URL |
| 通用 emoji 当功能图标 | ✅ 无 | 功能图标全为线性 SVG；`★/🧐` 仅作**语义数据字形**（§7.4 允许） |
| **圆角卡 + 左侧彩色边框套路** | ✅ **非违规** | 左色条**承载命中通道语义**（amber=★ / sky=🧐），由 `design-system §4` 明确规定——属**语义编码**而非装饰性套路 |
| 手绘/假人插画 | ✅ 无 | 作者纯文字；无头像、无插画 |
| 布局破碎/溢出 | ✅ 无 | 卡高 192px 落在 `design-system v1.1` 区间（comfortable 160–200px / compact 120–150px）；由 `.card-title` 两行截断（`262`）保证不裁切 `.card-foot`/`.myrating` |
| **文本对比度 WCAG AA** | ✅ **已修复** | `--accent-fill #2563EB`（白字 5.17:1）、`--text-tertiary #8B94A3`（5.9:1）已落地原型（`28`/`34`/`105`/`222`）；旧值 0 残留 |
| 完全无响应式 | ✅ 无 | 1280/1024/768 三断点齐备，右栏转抽屉、移动端筛选抽屉 |

**门控结论：P0 = 0 → Anti-Slop 通过。**

---

## 3. 令牌忠实度证据（v1.1 复扫）

对 CSS 段（`<style>` 内）做色值扫描，共 **70 个** `#hex/rgba` 字面量：
- **69 个**与 `design-system v1.1 §9.4` 令牌清单**逐一吻合**（含新增 `--accent-fill` `#2563EB`/`#1D4ED8`/`#1E40AF`、`--success-ink #062E20`、`--success-subtle`/`--danger-subtle`、提亮后 `--text-tertiary #8B94A3`/`#5F6773`）。
- **1 个非令牌字面量**：`rgba(0,0,0,.5)`（`#scrim` 遮罩）——遮罩层合理，可接受。
- **旧值清零**：`#6E7684`、`#6B7280`、字面量 `#fff` **均 0 残留**。
- **无自创色相 / 无第二品牌色。** ✅
- `ring:` 无效属性：**0 处**（现状缺陷已修复）。✅

---

## 4. 十态覆盖表（requirements-brief §6，逐条）

| # | 状态 | 覆盖 | 位置 / 证据 |
|---|------|------|------------|
| 1 | 默认 | ✅ 有 | 初始 `renderStream()`（`805`）；右栏未选中即空态（`renderDetail` `878` / 文案 `882`）；状态样张 ① |
| 2 | 筛选为空 | ✅ 有 | `computeList` 空 → `emptyState('filter')`（`814`/`830`）「没有匹配的论文」+ **清空筛选** 按钮 |
| 3 | 无数据日 | ✅ 有 | 日期下拉含 2 个无数据日；切换置 `noData` → `emptyState('nodata')`（`813`/`832`）「当日暂无数据…请先运行 `arxiv_daily.py fetch`」 |
| 4 | 未命中（其余） | ✅ 有 | `data-hit="none"`、无徽标、标题降 secondary、无左色条（`cardHTML` `772`）；筛选「其余」（`550`，默认**不勾选**，与实现一致）；仅命中/全部快捷；样张 ④ |
| 5 | 已打分 | ✅ 有 | `store.ratings` + localStorage；卡片 pips + 等宽数字；样张 ⑤；重进保留 |
| 6 | 已下载 | ✅ 有 | `store.downloads` 持久化；按钮转 success 实心勾 + `disabled`（`cardHTML`）；并入归档库；样张 ⑥ |
| 7 | 详情面板空态 | ✅ 有 | `.detail-empty`（`329`）文案 **「选择一篇论文查看详情」**（`882`，B2 已改中文） |
| 8 | 详情面板有值 | ✅ 有 | 全量字段 + 命中清单 + 逐条「为什么推荐」+ 打分 pips + 操作区（`renderDetail` `878` 起） |
| 9 | 洪峰日性能态 | ✅ 有 | 数据集切「洪峰日 300+」→ `getFlood()` ×3 = **384 篇**（`714`）；**虚拟滚动**（`renderWindow` `820`、scroll rAF） |
| 10 | 加载中/提交中 | ✅ 有（仅样张态） | 状态样张 ⑦ 骨架 + `[data-loading="true"]`（`renderSheet` 起）。**注**：browse 实时交互不可达，仅静态演示（P2） |

**覆盖结论：10/10 全覆盖。**（#10 仅样张静态呈现、#9 用 ×3 模拟放大——均符合 brief「原型可静态演示」口径）

---

## 5. 问题清单

### P0 —— 已关闭（3 项 · 复验通过，保留记录）

**P0-1｜文本对比度不达标（WCAG AA 4.5:1）** —— ✅ **已关闭**
- **原始问题（首版）**：`.btn-primary` 白字对 `--accent #3B82F6` = **3.68:1**；`--text-tertiary #6E7684` 用于 11px 小字仅 **3.6–3.9:1**。均 < AA。
- **处置**：DS 升 v1.1（新增 `--accent-fill #2563EB`、提亮 tertiary）；`prototype-builder` 同步进原型（`28`/`34`/`69`/`105`/`222`）。
- **复验（CDP）**：暗色 tertiary 5.87/5.40:1、亮色 5.72/5.14:1、主按钮白字 5.17:1，全 ≥AA ✅

**P0-2｜`bindControls()` 定义完整却从未被调用 —— 全站交互静默失效** —— ✅ **已关闭**
- **原始问题（首版）**：`bindControls()` 定义完整（绑定视图切换/主题/密度/数据集/筛选/搜索/重置/`⌘K`/卡片点击等约 16 个控件），但**启动段未调用它** → 交互全部无效，静态渲染正常故未察觉。
- **修复**：`prototype.html:1284` 新增 `bindControls();`（定义 `1159`，唯一调用）。
- **复验**：CDP 实测视图/主题/密度/数据集/筛选/选中全部生效；DOM 桩复跑无异常。✅

**P0-3（C1）｜`renderStream()` 漏 `renderDetail()` —— 右栏残留陈旧详情** —— ✅ **已关闭**
- **原始问题**：`renderStream()` 正常分支仅 `renderWindow();`；所有"清空选中"路径（重置/切类别/切日期/切数据集/搜索/取消勾选）执行后 `state.selectedIndex=-1`，但**右栏仍显示上一张论文详情**，与"无选中卡"状态自相矛盾。
- **修复**：`renderStream()` 内 `renderWindow();`（`817`）之后新增 `renderDetail();`（**`818`**）。
- **复验（CDP）**：重置后 `[data-selected="true"]`=0、`.detail-empty` 存在、右栏「选择一篇论文查看详情」；切类别→空态；切洪峰日→384 篇且右栏空态；**console 0 报错** ✅

### P1 —— 已关闭（1 项）

**P1-1｜`⌘K` 命令面板按钮未加「规划中」视觉区分** —— ✅ **已关闭**
- **原始问题**：`#cmdkBtn` 为常规实心图标按钮外观，与主题按钮同款，视觉不可区分。
- **修复**：builder 已改为**虚线描边**（`style="border-style:dashed"`）+ `<span class="planned">规划中</span>`（`482` 起）。
- **复验（CDP）**：`cmdkPlanned:true`、`densityPlanned:false` ✅
- **⚠ 更正说明（保留）**：此前误将「阅读密度切换」列入规划项——**错误**；密度切换是**已实现功能**（CDP 实测生效），不得加「规划中」。
- （其余规划项——收藏/笔记/agent 中文理由 `914–915`、画像滑块/编辑/删除、建议收件箱、归档检索、stat 统计——**均已用虚线「规划中」徽标正确标注** ✅）

### P2 —— 可选优化（剩余 8 项，非阻断）

> 注：C1 属**功能性缺陷**（状态不一致），故归为已关闭的 P0-3，不计入 P2；P2 维持 8 项。

1. **「为什么推荐」文案不统一**：卡片标签为「**推荐理由**」（`763`），详情面板为「**为什么推荐**」——建议统一为 brief §4 的「为什么推荐」；且该内容是「产品灵魂」却用最弱的 `--text-tertiary` 11px，建议提为 `--text-secondary`。
2. **归档预置数据与卡片「已下载」态不一致**：`DATA.archive` 预置 9 条（含 3 篇命中论文），但卡片「已下载」仅看 `store.downloads`，故初始状态**归档里有、卡片不显示已下载**。建议初始 `store.downloads` 与 `archive` 对齐（或注明归档为「历史库」）。
3. **令牌未消费 / 冗余代码**：`--card-pad`（`57`）定义后未被 `.card` 消费；主题切换处 `writeJSON('pg_theme',…)`（`1164`）紧接 `localStorage.setItem('pg_theme',…)` **重复且不一致**，删掉前者即可。
4. **命中数据偏少**：正常日 128 篇仅 **2 篇 ★ + 1 篇 🧐** 命中，brief §8-10 建议「正常日 ~30 篇命中」；演示面偏窄。建议补足样本（`hs/he` 逻辑正确，仅需扩充数据）。
5. **功能图标复用/语义**：`IC.book` 同时用于「收藏」「笔记」「来源」（`914`/`915`/`1051`）；danger toast 用 `IC.down` 当错误图标（`999`）——建议换 bookmark / note / alert-triangle。
6. **可交互尺寸**：`.rating .pip` 点击热区仅 22×6px，触屏不易点；建议加透明 padding 扩大热区（不改视觉）。
7. **无障碍细项**：卡片流容器 `.stream-inner`（`590`）建议补 `role="list"` 以匹配卡片的 `role="listitem"`（`783`）；#10 提交中态建议在 browse 模式提供「模拟请求中」开关。
8. **点击卡片未滚动居中**：仅键盘移动触发 `scrollToSelected()`，鼠标点击联动后不居中——可接受，供参考。

---

## 6. 关键验证明细

- **键盘契约（`973` 起）**：`j/↓`、`k/↑`、`1–5`→★、`Shift+1–5`→🧐（`e.code` 判 Digit1-5 + `shiftKey`）、`d` 下载、`o` 开 arXiv、`/` 聚焦搜索、`Escape` 失焦/关抽屉。
  - 边界钳制：`Math.max(0, Math.min(len-1, idx+dir))`——**实测连按 3 次 `k` 停在 index 0，不循环** ✅
  - 输入框不拦截：`typing` 判定 + `if (typing) return;`（位于视图/模式判断**之前**）——**实测焦点在 INPUT 时按 `j` 选中项不变** ✅
- **「为什么推荐」渲染**：`whyTags()`（`761`）用 `p.hs/p.he`（命中条目标题）+ `weightOf()`（`694`）；数据侧 `hs=['分布式计算与共识']`、`he=['LLM 与形式化/系统的交叉']` 与 `INTERESTS.title` 完全匹配。**实测 `cardHTML()` 输出同时含条目名与 `w=3`** ✅（历史缺陷已规避）
- **状态一致性（C1 复验）**：清空选中后右栏回到 `.detail-empty`（`882`），中栏 `[data-selected="true"]`=0 ✅
- **零外部请求**：`@import`/`<link>`/`fetch(`/`XMLHttpRequest`/`cdn|unpkg|jsdelivr|googleapis` 全部 0 命中 ✅
- **动效**：`--dur-*` 120/180/240ms 内；`prefers-reduced-motion` 归零时长（`84`）并停用骨架动画 ✅
- **真实浏览器（CDP · 主理人补做）**：Edge 无头 `--headless=new --remote-debugging-port`；视图/主题/密度/数据集/筛选/选中均生效，console **0 报错** ✅
- **DOM 桩复跑（修复后）**：启动路径含 `bindControls()`，无异常；`streamCount` 正常渲染「显示 128 / 128 篇」✅

---

## 7. 总判定

> ### **PASS**
> - 五维：**5 / 5 / 4 / 5 / 5 = 24 / 25**（全维 ≥3）
> - Anti-Slop：8 项通过、1 项不适用（左色条为语义编码）、**对比度项已修复** → 门控通过
> - **P0 = 0｜P1 = 0｜P2 = 8**（可选，非阻断）
> - 10 态覆盖 **10/10**；键盘契约、为什么推荐渲染、状态一致性、零外部请求、令牌忠实度均实测通过
>
> **判定依据**：三个 P0（对比度 / `bindControls()` 漏调用 / C1 `renderStream()` 漏 `renderDetail()`）与唯一 P1（`⌘K` 规划徽标）均已修复，并经 Edge 无头 + CDP 实测复验通过；五维全 ≥3、P0=0、P1=0，达成 PASS 全部条件。
>
> **遗留**：§5 P2 清单 8 条均为锦上添花项（文案统一、归档一致性、冗余代码、命中样本量等），**不构成阻断**。

---

*本报告仅审查、未改动原型或其他文件。行号已按最终 `prototype.html`（v1.1，约 1294 行）复标；所有字段/交互结论可回溯至真实源文件。*
